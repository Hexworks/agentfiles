# Plan — fixture missing-ac

Add an empty package `internal/oops` with a no-op test. The fixture is
intentionally trivial; the point is to fail Step 6.5a before the diff is
read.

See [description.md](./description.md).
