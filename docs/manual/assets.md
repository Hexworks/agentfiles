# Assets

Assets are files that you can synchronize between your projects to be used by agents including `agent-doc` files (like `AGENTS.md`, `CLAUDE.md`)
and skills.

Each _asset_ has its own folder even if there is only `1` file.
